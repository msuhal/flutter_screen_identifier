name: screen_identifier
description: A simple widget to adjust multiple values based on screen size or parent widget size
version: 0.0.1
repository: https://github.com/msuhal/flutter_screen_identifier
issue_tracker: https://github.com/msuhal/flutter_screen_identifier/issues

environment:
  sdk: '>=2.18.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
